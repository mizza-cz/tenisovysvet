{% macro icon(name, class = "", width = 24, height = 24) %}
    <svg class="{{ class }}" width="{{ width }}" height="{{ height }}" focusable="false" aria-hidden="true">
      <use xlink:href="#{{ name }}"></use>
    </svg>
{% endmacro %}
